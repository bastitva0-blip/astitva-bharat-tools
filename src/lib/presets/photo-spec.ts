// Legacy re-export — the canonical type now lives in spec-db/schema.ts.
// Kept for the components that import `PhotoSpecPreset` from this path.

export type { PhotoSpec as PhotoSpecPreset } from "@/lib/spec-db/schema";
